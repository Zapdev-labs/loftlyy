[build]
builder = "railpack"
# Build MCP server bundle first, then Next.js
buildCommand = "pnpm install && npm --prefix mcp-server install && npm --prefix mcp-server run build && pnpm exec next build"

[deploy]
startCommand = "node server.mjs"
healthcheckPath = "/mcp/health"
healthcheckTimeout = 60
