[build]
builder = "railpack"
# Call next build directly to skip the prebuild asset-validator script,
# which expects local SVG files that live in a CDN (not the git repo).
buildCommand = "pnpm install && pnpm exec next build"

[deploy]
startCommand = "pnpm exec next start"
healthcheckPath = "/"
healthcheckTimeout = 60
